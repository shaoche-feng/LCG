gymnasium==0.29.1
ale-py==0.9.0
h5py==3.11.0
huggingface-hub==0.17.2
hydra-core==1.3
numpy==1.26.4
opencv-python==4.10.0.84
pillow==10.3.0
pygame==2.5.2
# torch 2.7.1 is the version validated against dm_control/mujoco below on this project.
# For a CUDA build, install from the PyTorch index matching the server's CUDA driver, e.g.:
#   pip install torch==2.7.1 --index-url https://download.pytorch.org/whl/cu128
# (substitute cu121/cu124/etc. for an older driver); a plain `pip install -r requirements.txt`
# will otherwise pull torch's default PyPI CUDA build, which also works but may not match
# the exact +cu128 build this project was tested with on a Blackwell-generation GPU.
torch==2.7.1
torcheval==0.0.7
tqdm==4.66.4
wandb==0.17.0

# DM Control Suite support (see README's "DM Control Suite support" section).
# mujoco is pulled in automatically by dm_control but pinned here for reproducibility.
dm_control==1.0.44
mujoco==3.11.0
